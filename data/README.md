# Local driver database

The VituixCAD driver database is not distributed with this repository.

Obtain or export it separately under its applicable terms and place the local
file at:

```text
data/VituixCAD_driver_db.txt
```

That path is ignored by Git. Do not commit or redistribute the database.
